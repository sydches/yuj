"""Names owned by the task-artifact directory, not by arbitrary workspaces."""

PROMPT_NAME = "prompt.txt"
CHECKPOINT_NAME = "checkpoint.json"
METRICS_NAME = "metrics.json"

TASK_ARTIFACT_NAMES = (PROMPT_NAME, CHECKPOINT_NAME, METRICS_NAME)
