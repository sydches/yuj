Replace the exact source of one existing code or Markdown cell in a Jupyter notebook without reserializing the notebook. Read the notebook first. Pass exactly one selector: zero-based `cell_index` or `cell_id`. `old_source` must exactly match the complete current cell source. Yuj preserves cell order, IDs, metadata, outputs, attachments, and unrelated JSON bytes. Configured edit checks may revert the change if they fail.
