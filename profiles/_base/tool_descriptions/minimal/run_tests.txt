Run the task test command established from project/runtime observations or an explicit runner setting, using its configured flags and timeout. Use when that command matches the intended check. Args: path (file or directory; omitted means the runner's default scope), k (test-name filter), last_failed (request prior failures). Filter support depends on the selected runner; unsupported filters may be ignored or rejected. Leave unused filters unset. Structured output reports the runner, status and exit code when available. A timeout, unavailable runner or unresolved selection is not a test pass or failure. Failure output may include source context. If selection is unresolved or you need different command arguments, use the project's observed instructions and bash. A passed result describes the checks actually run; it does not prove the whole task is complete.
