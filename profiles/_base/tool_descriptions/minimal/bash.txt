Run a shell command; returns stdout+stderr. Output is normalized (ANSI stripped, blank-line and duplicate-line runs collapsed, secrets redacted) and head/tail-truncated when it exceeds the context budget. Append quiet flags to verbs that emit large logs (e.g. `pip install -q`, `make -s`, `npm install --loglevel=error`) — for some of these the harness already auto-appends a quiet flag. Prefer typed tools when available: `read` instead of `cat`/`head`/`tail`, `glob` instead of `find`, `grep` instead of `rg`/`grep`, `run_tests` instead of pytest invocations, and `apply_patch`/`edit`/`write` instead of shell redirection. If the active tool profile exposes only `bash` and `done`, mutate source files through bash with a short `python`, `perl`, `sed -i`, `cat >file`, or `tee` command, then run targeted verification.
