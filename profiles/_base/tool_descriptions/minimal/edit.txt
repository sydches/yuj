Replace the first occurrence of `old_str` with `new_str` in a file. Read the file first; when copying old_str from read output, strip the leading `<N>: ` line-number prefix and preserve the exact indentation that follows. `old_str` must be non-empty. On strict miss, returns `ERROR: old_str not found in <path>` followed by a `<candidates>` block of ranked near-miss spans the model can copy verbatim and retry. Configured post-edit checks may revert the edit if they fail. Errors on binary / non-UTF-8 files — use `write` or `apply_patch` instead.
