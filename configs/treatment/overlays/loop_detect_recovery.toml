# Explicit completed-observation policy. Historical path retained for overlays.

[loop]
duplicate_guard_enabled = true
duplicate_warn_count = 2
duplicate_abort = 0

[prompts]
loop_detect_recovery = "<system-reminder>The last {streak} calls repeat the same request. Name what you still need and change the query or range.</system-reminder>"
